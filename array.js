//Return the First Element in an Array 

//Function Declaration
function arr(n) {
  // Storing the first item in a variable First
    let First = n[0]; 
    return First;  
}

//Array declaration and Function Calling
let Result = arr([ "Hello JS", 2.5, 3, 4, 5 ]);

// Printing the output to screen
console.log(" First element in the array = ", Result);